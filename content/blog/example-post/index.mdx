---
title: "Hello, Log"
excerpt: "The first post — and a quick tour of what this blog can do."
coverImage: "./cover.jpg"
coverImageAlt: "Placeholder cover — replace with your own image before publishing."
tags: ["meta"]
draft: true
---

This is the first post on the log. It's written in **MDX**, so regular Markdown works exactly as you'd expect — headings, lists, links, images, code fences, tables, blockquotes.

## Formatting reference

A [link to the homepage](/), some `inline code`, and a fenced code block:

```ts
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
```

> A blockquote, for when a quote or an aside earns its own visual weight.

| Feature | Status |
| --- | --- |
| Cover images | Done |
| Syntax highlighting | Done |
| Tags | Done |

Replace this file's content, drop a real `cover.jpg` next to it, flip `draft` to `false`, and publish:

```bash
npm run blog:publish -- content/blog/example-post
```
